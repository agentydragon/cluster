# Use shell.nix to get latest kubeseal, tflint, and other tools
use nix

# Set talos config path
export TALOSCONFIG="$PWD/terraform/infrastructure/talosconfig.yml"

# Set kubeconfig path (generated by terraform)
export KUBECONFIG="$PWD/terraform/infrastructure/kubeconfig"

# kubeseal configured to use standard defaults (kube-system namespace)
