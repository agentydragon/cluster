apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.yaml
  - helmrelease.yaml
  - element-web.yaml
# Matrix namespace created by k8s/authentik-blueprint/matrix-secret/ (ensures namespace exists before blueprint creates resources in it)
# Matrix OIDC/SSO configuration is handled by k8s/authentik-blueprint/matrix-provider/ (Terraform) and k8s/authentik-blueprint/matrix-secret/ (ExternalSecret)
