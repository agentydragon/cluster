apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - oauth-config-job.yaml
# Configures Gitea OAuth integration with Authentik
# Requires: Gitea running + Authentik certificate ready
