apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # CNI must be first - everything else depends on it
  - cilium
  # Core networking components
  - cert-manager
  - metallb
  - metallb-config
  # Services that depend on LoadBalancer
  - ingress-system
  - powerdns
