apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # SealedSecret will be added here after generation:
  # - vault-bootstrap-sealed.yaml
  - vault-release.yaml
  - external-secrets.yaml
  - cluster-secret-store.yaml
  - ../sso-secrets.yaml
  - ../vault-config.yaml