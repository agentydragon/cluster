apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Bank-Vaults Vault instance (operator deployed separately)
  - rbac.yaml
  - instance.yaml
  - certificate.yaml
  - ingress.yaml
