apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Bank-Vaults Vault instance (operator deployed separately)
  - rbac.yaml
  - instance.yaml
  - ingress.yaml
  # TLS certificates - split internal (self-signed) and external (Let's Encrypt)
  - internal-tls-issuer.yaml
  - internal-tls-certificate.yaml
  - certificate.yaml # External ingress certificate (Let's Encrypt)
