apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Cilium CNI (Flux takes over from Terraform)
  - cilium-repo.yaml
  - cilium-release.yaml
  # MetalLB with proper dependency ordering
  - metallb-repo.yaml
  - metallb-release.yaml
  - metallb-config.yaml
