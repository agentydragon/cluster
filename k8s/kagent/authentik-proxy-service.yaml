apiVersion: v1
kind: Service
metadata:
  name: authentik-outpost-proxy
  namespace: kagent
spec:
  type: ExternalName
  externalName: authentik-server.authentik.svc.cluster.local
  ports:
    - name: http
      port: 80
      targetPort: 80
